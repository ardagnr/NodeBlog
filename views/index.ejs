<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Node Blog |<%=title%></title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body class="bg-light">

    <%- include('../inc/nav.ejs') %>

    <div class="container mt-4">

        <section class="text-center mb-4">
            <h1 class="display-4 text-primary">Yazılar</h1>
            <p class="lead text-muted">Blog yazılarına göz atın</p>
        </section>


        <section>
            <ul class="list-group">
                <% blogs.forEach(blog => { %>
                    <li class="list-group-item mb-2">
                        <h5>
                            <a href="/blog/<%= blog._id %>" class="text-decoration-none text-dark">
                                <%= blog.title %>
                            </a>
                        </h5>
                        <p class="text-muted"><%= blog.short %></p>
                    </li>
                <% }) %>
            </ul>
        </section>
    </div>
</body>
</html>